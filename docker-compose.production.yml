version: '3'

volumes:
  pg_data:
  static:

services:
  db:
    restart: always
    image: postgres:13.10
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data
  backend:
    restart: always
    image: pipchansky/taski_backend
    depends_on:
      - db
    env_file: .env
    volumes:
      - static:/backend_static
  frontend:
    restart: always
    image: pipchansky/taski_frontend
    env_file: .env
    command: cp -r /app/build/. /frontend_static/
    volumes:
      - static:/frontend_static
  gateway:
    restart: always
    image: pipchansky/taski_gateway
    depends_on:
      - db
    env_file: .env
    volumes:
      - static:/staticfiles/
    ports:
      - 8001:80
